; 注意：不要在此写死 open_basedir 指向某个域名目录（会导致站点换目录后全部 404）
; open_basedir 由宝塔面板「网站 → 设置 → 网站目录 → 防跨站攻击(open_basedir)」按站点自动生成
upload_max_filesize=200M
post_max_size=200M
max_execution_time=300
max_input_time=300
memory_limit=256M
